import styled from "styled-components";

export const ModalWraper = styled.div`
    position:fixed;
    top:0;
    left:0;
    right:0;
    bottom:0;
    z-index:1002;
    background:rgba(0,0,0,0.5);
`;